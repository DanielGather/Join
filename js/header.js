/**
 * Toggles the visibility of a pop-up element.
 * Adds or removes the `d-none` class to show or hide the pop-up.
 */
function showPopUp(){
    document.getElementById("popUp").classList.toggle("d-none");
}